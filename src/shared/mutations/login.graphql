mutation login($data: LoginUserInput!) {
  login(data: $data) {
    access_token,
    expiresAt,
    user {
      uuid,
      username,
      email,
      type,
      firstName,
      lastName,
      avatar
    }
  }
}