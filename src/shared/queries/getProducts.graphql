query getProducts($where: ProductWhereInput!) {
  getProducts(where: $where) {
    name
    description
    price
    images
    uuid
    category {
        name
      }
    vendorProduct{
      vendor{
        name
      }
    }
  }
}